# PLAN.md — AI Multi-Agent 代码生成平台 · 产品需求文档

> 版本：v0.2 · 状态：待 Review · 日期：2026-04-26

---

## 一、产品定位

### 1.1 一句话定义

用自然语言描述你的想法，一支 AI Agent 团队帮你生成可运行的全栈代码，并同步到你的 GitHub 仓库。

### 1.2 解决什么问题

| 痛点 | 现状 | 本产品解法 |
|------|------|-----------|
| 从想法到代码跨越太大 | 需要自己写 PRD → 拆任务 → 逐一实现 | AI Agent Team 自动完成全流程 |
| 单个 AI 生成代码质量不稳定 | 一个 LLM 一次性生成，缺乏规划和检验 | 多 Agent 分工：先计划，再执行，再审查 |
| 生成的代码难以落地 | 代码片段、无工程结构 | 生成完整项目结构，写入 Git 仓库，可直接运行 |
| 模型供应商绑定 | 只能用某一家模型 | 可配置，支持内部 CatPaw 和外部 Claude / GPT |

### 1.3 目标用户（MVP）

- 有产品想法、希望快速验证 MVP 的个人开发者 / 产品经理
- 内部（美团）希望快速搭建工具的业务同学

---

## 二、MVP 功能范围

### 2.1 核心链路

```
用户输入需求描述 + 选择技术栈模板
        ↓
Agent Team 流水线执行（流式输出，实时可见）
  ① PM Agent      → 功能列表 + 用户故事
  ② Architect Agent → 架构 + Schema + API 定义
  ③ Engineer Agent  → 逐文件生成完整代码
  ④ QA Agent       → 基础代码检查
        ↓
生成结果展示（文件树 + 代码预览 + 语法高亮）
        ↓
一键下载 zip  /  自动推送到新建的 GitHub 仓库
```

### 2.2 MVP 功能清单

#### 用户系统
- **无需注册登录**（单用户模式，直接使用）
- 历史任务列表（本地存储或数据库，可查看之前的生成记录）
- 设置页：配置 AI 模型（全局）+ GitHub Personal Access Token

#### 需求输入
- 文本框输入需求描述（支持多行，Markdown 友好）
- 技术栈模板选择：Vue 3 + Node.js / React + Express / 纯前端 Vue 3
- 单轮输入，提交后进入执行（MVP 不支持中途修改）

#### Agent 工作流
- **PM Agent**：拆解功能列表 + 用户故事（JSON 输出）
- **Architect Agent**：系统架构 + 数据库 Schema + API 接口定义
- **Engineer Agent**：逐文件生成完整可运行代码
- **QA Agent**（简化版）：检查代码问题，输出问题清单（不自动修复）
- 流水线顺序执行，不支持中途中断或修改

#### 执行进度展示
- 步骤进度条（显示当前执行到哪个 Agent）
- 流式打字效果（实时展示 Agent 输出内容，类 ChatGPT 体验）
- 结果页可回顾每个 Agent 的完整输出日志

#### 代码查看
- 文件树（生成项目的完整目录结构）
- 代码内容预览（点击文件查看，只读）
- 语法高亮（Monaco Editor）
- 一键下载 zip 压缩包

#### GitHub 集成
- 用户在设置页填写 GitHub Personal Access Token
- 生成完成后，自动在用户账号下创建新仓库
- 将所有生成文件作为一个 commit 推送
- 展示仓库链接和 commit 链接

#### 模型配置
- 设置页选择全局模型，所有 Agent 使用同一模型
- 支持的模型：
  - 🏠 内部：CatPaw（catclaw-proxy-model）
  - 🌐 外部：Claude（用户填 API Key）
  - 🌐 外部：OpenAI GPT-4o（用户填 API Key）

### 2.3 明确不做（MVP 范围外）

| 功能 | 后续迭代 |
|------|---------|
| 用户注册 / 登录系统 | P1 |
| 各 Agent 独立配置模型 | P1 |
| 中途中断 / 修改 Agent 产物 | P1 |
| 在线编辑代码 | P1 |
| 推送到已有仓库 | P1 |
| GitHub OAuth 授权 | P1 |
| GitLab 支持 | P2 |
| 一键部署（Vercel 等） | P2 |
| 竞速模式（多版本并行生成） | P2 |
| 上传参考截图 | P2 |
| 多轮补充需求 | P2 |

---

## 三、用户交互流程

### 主流程（Happy Path）

```
[首页] 
  → 点击"新建项目"
  → [创建页] 
      填写需求描述（文本框）
      选择技术栈模板
      点击"开始生成"
  → [执行页]
      实时流式展示 Agent 工作进度
      步骤：PM → Architect → Engineer → QA
  → [结果页]
      文件树 + 代码预览
      "下载 zip" 按钮
      "推送到 GitHub" 按钮（需设置 Token）
      查看 GitHub 仓库链接
```

### 异常流程

| 场景 | 处理方式 |
|------|---------|
| 模型调用失败 | 自动重试 2 次，失败后展示错误提示，任务标记失败 |
| GitHub 推送失败 | 提示检查 Token 权限，不影响查看/下载代码 |
| 未配置 GitHub Token | 隐藏推送按钮，提示去设置页配置 |
| 未配置模型 / API Key | 进入创建页前检查，未配置则跳转设置页 |

---

## 四、页面清单

| 页面 | 路由 | 核心内容 |
|------|------|---------|
| 首页 | `/` | 项目简介 + 历史任务列表 + "新建项目"按钮 |
| 创建任务 | `/tasks/new` | 需求输入 + 技术栈选择 |
| 任务执行 | `/tasks/:id` | Agent 进度 + 流式输出（执行中）|
| 任务结果 | `/tasks/:id` | 文件树 + 代码预览 + 下载/推送（执行完）|
| 设置 | `/settings` | 模型配置 + GitHub Token |

> 执行中和结果复用同一路由，根据任务状态切换展示。

---

## 五、非功能需求

| 指标 | 目标 |
|------|------|
| 单次生成时长 | ≤ 3 分钟（中等复杂度，约 10 个文件） |
| 模型调用失败重试 | 自动重试 2 次 |
| 代码生成质量底线 | 生成的前端项目可以 `npm install && npm run dev` 跑起来 |
| 浏览器兼容 | Chrome / Edge 最新版（不做 IE / Safari 兼容） |

---

## 六、MVP 验收标准

- [ ] 输入需求描述 + 选技术栈，点击"开始生成"，能看到 4 个 Agent 依次工作
- [ ] 流式打字效果正常，能实时看到 Agent 输出内容
- [ ] 生成结束后，文件树和代码预览正常展示
- [ ] 可以下载 zip 压缩包
- [ ] 配置 GitHub PAT 后，能自动创建仓库并推送代码
- [ ] 设置页可以切换模型（CatPaw / Claude / GPT-4o）
- [ ] 历史任务列表可以查看之前的生成记录
- [ ] 整个流程纯 Web，不需要命令行
